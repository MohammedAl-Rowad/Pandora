export type ChartData = Array<Data>;
export type Data = { name: string; value: number };
export type LineData = Array<{ name: string; series: Array<ChartData> }>;
export const allProjects = '🚧 ALL PROJECTS 🚧';
